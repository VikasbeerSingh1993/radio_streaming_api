spring.application.name=radio-streaming-api

# Railway injects PORT. Fall back to 8080 locally.
server.port=${PORT:8080}

# Bootstrap only: opens Mongo so app_credentials can be read.
# All other secrets (MySQL, Gmail, B2, GEO, admin password) live in Mongo / code catalog — not on Railway.
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://USERNAME:PASSWORD@cluster0.example.mongodb.net/divine_bliss_streaming}
spring.data.mongodb.database=divine_bliss_streaming

spring.jackson.serialization.write-dates-as-timestamps=false
spring.data.mongodb.auto-index-creation=true

app.cache.ttl=24h

# Process crypto defaults (not connection credentials). Prefer setting once in code/deploy; not app secrets.
app.jwt.secret=${JWT_SECRET:radio-streaming-admin-jwt-secret-change-me-32b}
app.jwt.expiration-ms=28800000
app.credentials.encrypt-key=${CREDENTIALS_ENCRYPT_KEY:${JWT_SECRET:radio-streaming-admin-jwt-secret-change-me-32b}}
app.mail.log-otp=${MAIL_LOG_OTP:false}
